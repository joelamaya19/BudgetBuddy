

const AccountList = ({
// props
}) => {
    if (!accounts.length) {
        return <h3>No accounts created.</h3>;
    }

    // How its going to look in the front end?
    return (
        <div>

        </div>
    );
};

export default AccountList;